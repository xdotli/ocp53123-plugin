import { Alert } from '../api/common-types';
import { LaunchModal } from '../app/modal-support/ModalProvider';
import { Extension, ExtensionDeclaration, CodeRef } from '../types';
export declare type AlertAction = ExtensionDeclaration<'console.alert-action', {
    alert: string;
    text: string;
    action: CodeRef<(alert: Alert, launchModal: LaunchModal) => void>;
}>;
export declare const isAlertAction: (e: Extension) => e is AlertAction;

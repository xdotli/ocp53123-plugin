import * as React from 'react';
import { CheckCircleIcon, InfoCircleIcon, ExclamationCircleIcon, ExclamationTriangleIcon, } from '@patternfly/react-icons';
import * as classNames from 'classnames';
import './icons.scss';
/**
 * Component for displaying a green check mark circle icon
 * @param {string} [className] - (optional) additional class name for the component
 * @param {string} [title] - (optional) icon title
 * @param {string} [size] - (optional) icon size: ('sm', 'md', 'lg', 'xl')
 * @example
 * ```tsx
 * <GreenCheckCircleIcon title="Healthy" />
 * ```
 */
export const GreenCheckCircleIcon = ({ className, title, size }) => (React.createElement(CheckCircleIcon, { "data-test": "success-icon", size: size, className: classNames('dps-icons__green-check-icon', className), title: title }));
/**
 * Component for displaying a red exclamation mark circle icon
 * @param {string} [className] - (optional) additional class name for the component
 * @param {string} [title] - (optional) icon title
 * @param {string} [size] - (optional) icon size: ('sm', 'md', 'lg', 'xl')
 * @example
 * ```tsx
 * <RedExclamationCircleIcon title="Failed" />
 * ```
 */
export const RedExclamationCircleIcon = ({ className, title, size, }) => (React.createElement(ExclamationCircleIcon, { size: size, className: classNames('dps-icons__red-exclamation-icon', className), title: title }));
/**
 * Component for displaying a yellow triangle exclamation icon
 * @param {string} [className] - (optional) additional class name for the component
 * @param {string} [title] - (optional) icon title
 * @param {string} [size] - (optional) icon size: ('sm', 'md', 'lg', 'xl')
 * @example
 * ```tsx
 * <YellowExclamationTriangleIcon title="Warning" />
 * ```
 */
export const YellowExclamationTriangleIcon = ({ className, title, size, }) => (React.createElement(ExclamationTriangleIcon, { size: size, className: classNames('dps-icons__yellow-exclamation-icon', className), title: title }));
/**
 * Component for displaying a blue info circle icon
 * @param {string} [className] - (optional) additional class name for the component
 * @param {string} [title] - (optional) icon title
 * @param {string} [size] - (optional) icon size: ('sm', 'md', 'lg', 'xl')
 * @example
 * ```tsx
 * <BlueInfoCircleIcon title="Info" />
 * ```
 */
export const BlueInfoCircleIcon = ({ className, title }) => (React.createElement(InfoCircleIcon, { className: classNames('dps-icons__blue-info-icon', className), title: title }));
//# sourceMappingURL=icons.js.map
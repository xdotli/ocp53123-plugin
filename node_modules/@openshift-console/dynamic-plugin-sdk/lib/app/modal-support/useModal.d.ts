import { LaunchModal } from './ModalProvider';
declare type UseModalLauncher = () => LaunchModal;
/**
 * A hook to launch Modals.
 *
 * ```tsx
 * const AppPage: React.FC = () => {
 *  const [launchModal] = useModal();
 *  const onClick = () => launchModal(ModalComponent);
 *  return (
 *    <Button onClick={onClick}>Launch a Modal</Button>
 *  )
 * }
 * ```
 */
export declare const useModal: UseModalLauncher;
export {};
